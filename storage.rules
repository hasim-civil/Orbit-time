rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Each user can read and write only their own profile photo.
    match /profile_photos/{uid}.jpg {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
